node_modules/
dist/

.env
.env.local
.env.*.local

supabase/.temp/
supabase/.branches/

# 국토부 실거래가 원본/수집 CSV — 대용량, 파이프라인으로 재생성 가능
scripts/data/

# 단지×면적 룩업 — 3MB+, CSV에서 재생성 가능. /register에서 쓰게 되면 lazy-load 권장
src/data/complexLookup.json

# scripts/import-trades-csv.mjs + generate-properties.mjs 의 중간 산출물.
# 데이터는 모두 Supabase 에 들어가고, 시드 SQL은 supabase/seed/ 에 커밋됨.
src/data/marketData.json
src/data/properties.generated.json

# build-price-trends.mjs 산출물 — Supabase price_trends 로 import. 재생성 가능.
scripts/output/

# Vercel 로컬 작업 폴더
.vercel/

# Claude Code 로컬 설정
.claude/

# 일회성 스크린샷 캡처 헬퍼 (포트폴리오 무관)
scripts/_capture_content.ps1
scripts/_foreground.ps1

# 지원사업 보고서 초안 (개인 문서)
지원사업_*.md
