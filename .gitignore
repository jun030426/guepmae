node_modules/
dist/

.env
.env.local
.env.*.local

supabase/.temp/
supabase/.branches/

# 국토부 원본 CSV — 너무 크고 scripts/import-trades-csv.mjs로 재생성 가능
scripts/data/*.csv

# 단지×면적 룩업 — 3MB+, CSV에서 재생성 가능. /register에서 쓰게 되면 lazy-load 권장
src/data/complexLookup.json

# Vercel 로컬 작업 폴더
.vercel/

# Claude Code 로컬 설정 (사용자별 권한 등 개인 설정)
.claude/settings.local.json
